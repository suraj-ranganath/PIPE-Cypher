\begin{table}[t]
\centering
\small
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lrrp{0.56\columnwidth}}
\toprule
Signature ID & Count & Share & Canonical preview \\
\midrule
f15b226863 & 250 & 0.083 & match (<var>:person \{personname: <str>\})-[:own\_account]->(<var>:account)-[t:transfer\_to]->(:account) return distinct sum(<var>.amount) as to \\
06f23c9775 & 250 & 0.083 & match (<var>:person \{personname: <str>\})-[:own\_account]->(<var>:account) return distinct <var>.accountid as accountid, <var>.accounttype as  \\
15552613ff & 250 & 0.083 & match (<var>:person \{personname: <str>\})-[:own\_account]->(<var>:account) return distinct count(distinct a) as accountcount \\
375606ce8c & 250 & 0.083 & match (<var>:person \{personname: <str>\})-[:own\_account]->(<var>:account)-[:transfer\_to]->(<var>:account) return distinct <var>.accountid as  \\
4a1918b4a5 & 248 & 0.083 & match (<var>:person \{personname: <str>\})-[:own\_account]->(<var>:account)-[t:transfer\_to]->(:account) with src, sum(<var>.amount) as totalamo \\
f2374ff7d1 & 226 & 0.075 & match (<var>:person \{personname: <str>\})-[:own\_account]->(<var>:account) where not (a)-[:transfer\_to]->(:account) return distinct <var>.acco \\
aa411b54b1 & 158 & 0.053 & match (<var>:account \{accountid: <str>\})-[:transfer\_to]->(:account) return distinct count(distinct src) > <num> as hasoutgoingtransfer \\
f384aa977a & 137 & 0.046 & match (<var>:person \{personname: <str>\})-[:own\_account]->(<var>:account)-[:transfer\_to*<num>..<num>]->(<var>:account) return distinct <var>. \\
\bottomrule
\end{tabular}
}
\caption{Top canonical query signatures in the full export. Literals, numbers, and variables are normalized before counting, so this table measures template concentration rather than raw value reuse.}
\label{tab:query_signature_concentration}
\end{table}
